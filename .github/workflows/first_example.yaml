name: My f workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repor
              run: actions/checkout@v4
            - name: welcome message
              run: echo "my first workflow"
            - name: List file
              run: ls
            - name: Read File
              run: cat README.md


